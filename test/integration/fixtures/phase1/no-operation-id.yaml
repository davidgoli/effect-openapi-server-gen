# Invalid spec - missing operationId (should fail)
# Exercises: validation of required operationId field
openapi: 3.1.0
info:
  title: Invalid API
  version: 1.0.0

paths:
  /test:
    get:
      summary: This operation is missing operationId
      responses:
        "200":
          description: Success
